const Pricing = (function () {
    "use strict"
    
    const init = function (settings) {
    
    }
    
    return {
        detail: {},
        all: new Map(),
        init: function (settings) {
            init(settings)
        },
    }
    
})()
